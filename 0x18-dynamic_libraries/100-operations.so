#include <stdlib.h>

int add(int a, int b) {
    return (a + b);
}

int sub(int a, int b) {
    return (a - b);
}

int mul(int a, int b) {
    return (a * b);
}

int div(int a, int b) {
    return (a / b);
}

int mod(int a, int b) {
    return (a % b);
}

int main(void) {
    return (0);
}

// Create dynamic library
gcc -fPIC -c 100-operations.c 
gcc -shared -o 100-operations.so 100-operations.o
